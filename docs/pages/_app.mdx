import "@/styles.css";
import { Provider } from "@/components/Switchers";

export default function App({ Component, pageProps }) {
  return (
    <Provider>
      <Component {...pageProps} />
    </Provider>
  );
}

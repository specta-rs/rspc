Check everything is moved from the old repo!
Fix warnings
Fix jump to definition with the new syntax




// #[derive(Deserialize)]
// struct Parameters<'a> {
//     #[serde(borrow)]
//     input: &'a RawValue,
// }
//
//
// let params: Parameters = serde_urlencoded::from_str(
//     req.uri().query().unwrap_or_default(),
// )
// .unwrap(); // TODO: Error handling
// println!("{:?}", params.input);

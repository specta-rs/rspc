import { render } from "solid-js/web";

// TODO: Generate this from the Rust code
export interface PlaygroundConfig {}

function App() {
  // TODO: Get configuration from script in DOM

  return <h1>Hello World </h1>;
}

render(App, document.body);
